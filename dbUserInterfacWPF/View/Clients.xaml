<UserControl x:Class="Market.WPF.View.Clients"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Market.WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<Grid Margin="12">

		<TextBlock Text="CLIENTS:"
                       Foreground="#DBDBDB"
                       FontSize="12"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       FontFamily="/Fonts/#Rubik Medium"
                       Margin="0,10,160,0" />

		<Border
			Grid.Row="1"
			Background="#272B2F"
			CornerRadius="5"
			Padding="1">

			<DataGrid 
			HorizontalAlignment="Center" 
			Height="382" Grid.Row="1" 
			VerticalAlignment="Center" 
			Width="756"
			Background="#272B2F"
				
			d:ItemsSource="{Binding Items}">
				<DataGrid.Columns>
					<DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*"/>
					<DataGridTextColumn Binding="{Binding PhoneNumber}" Header="Phone" Width="*"/>
					<DataGridTextColumn Binding="{Binding Email}" Header="Email" Width="*"/>
					<DataGridTextColumn Binding="{Binding CreatedAt}" Header="CreatedAt" Width="*"/>
					<DataGridTextColumn Binding="{Binding UpdatedAt}" Header="UpdatedAt" Width="*"/>
				</DataGrid.Columns>
			</DataGrid>

		</Border>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

	</Grid>

</UserControl>