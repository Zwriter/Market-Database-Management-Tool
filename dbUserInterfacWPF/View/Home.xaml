<UserControl x:Class="Market.WPF.View.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
	
	<Grid Margin="12">
		<StackPanel>
			<TextBlock Text="Home" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>
			<Button Content="Load" Command="{Binding LoadCommand}" Width="100" Margin="0,0,0,8"/>
			<StackPanel Orientation="Horizontal" Margin="0,8,0,0">
				<ItemsControl ItemsSource="{Binding SalesByCategory}">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Key}"/>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
				<Separator Width="20"/>
				<ItemsControl ItemsSource="{Binding StockByFactory}">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Key}"/>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>